import styled from "styled-components";

export const Wrapper = styled.div`
  text-align: right;
  font-family: monospace;
  color: ${({ theme }) => theme.color.boulder};
  margin-top: 10px;
  scroll-margin-right: 10 px;
`;
